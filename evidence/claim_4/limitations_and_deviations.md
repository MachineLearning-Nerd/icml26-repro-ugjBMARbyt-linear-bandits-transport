# Claim 4 limitations and deviations

The experiment targets the literal v1 premise and the specific fixed-order
algorithm used to prove the corollary. It does not dispute the standard
finite-dimensional OFUL rate under the stronger, conventional assumption that
the true model really has dimension `N`.

Finite support is the paper's own named application. Cost values admit a
continuous extension from the separated finite support.
