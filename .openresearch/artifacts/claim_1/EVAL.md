# Claim 1 evaluation

The corrected specialization is accepted only if both the Parseval and
transport-pairing residuals are below `1e-12` and the literal-normalization
negative control has residual above `1.8`. Even on success, the literal v1
claim verdict remains `BLOCKED` because the operator was changed.
