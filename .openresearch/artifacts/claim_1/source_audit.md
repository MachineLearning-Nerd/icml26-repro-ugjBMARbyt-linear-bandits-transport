# Claim 1 source audit

ArXiv v1 Equation (7) defines its transform by integration against an arbitrary
reference probability measure and calls that operator an `L2(rho)` isometry.
This experiment instead evaluates the standard unitary transform on the finite
group `Z_2 x Z_2`. The changed normalization is explicit and prevents this
result from being credited as verification of the literal claim.
