# Claim 5 evaluation

`FALSIFIED` requires the literal `q=4` assumption, a finite tail-bound residual
above 1.4, and an independently checked `l2` sequence that satisfies
Assumption 3 but has divergent `l1` tail. The negative control requires the
paper-derived tail assertion and must reject the admitted finite sequence.

The pre-entry slope is diagnostic only; it is not used by itself to assign the
verdict.
